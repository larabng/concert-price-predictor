pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
xgboost>=2.0
streamlit>=1.30
plotly>=5.18
matplotlib>=3.7
seaborn>=0.13
joblib>=1.3
tqdm>=4.66
openai>=1.0
Pillow>=10.0
# transformers and torch are optional (Approach A / DistilBERT).
# Install locally with: pip install transformers torch
# Not required for the default keyword-based Approach B.
